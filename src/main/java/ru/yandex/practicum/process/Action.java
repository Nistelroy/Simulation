package ru.yandex.practicum.process;

import ru.yandex.practicum.objects.Emtity;
import ru.yandex.practicum.objects.notMouvingObjects.Grass;

public class Action {
    public void render(Map map) {
        for (int x = 8; x >= 1; x--) {
            String line = "";
            for (int y = 1; y <= 8; y++) {
           //     if (map.maps.get(coordinates1) == null) {
             //       System.out.println(map.maps.get(coordinates1).coordinates.HEIGHT);
              //      System.out.println(map.maps.get(coordinates1).coordinates.WIDTH);
            }

        }
    }
    private String getSpringForEmptySquare(Coordinates coordinates) {

        return " ";
    }
}
